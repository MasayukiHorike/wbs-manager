# CHANGELOG — WBS Manager

開発の経緯と各バージョンの変更内容を記録します。

---

## v1.0.0 — 初回リリース

**基本的なWBS管理ツールとして実装**

- タスクの階層構造（インデント）表示
- 担当者・期日の管理
- 進捗率・ステータス管理（未着手 / 進行中 / 完了 / ブロック中）
- ガントチャート表示（日単位）
- テキスト検索・ステータス・担当者フィルタ
- CSVエクスポート（BOM付きUTF-8）
- ライトテーマ（クリーン）デザイン

---

## v1.1.0 — サイドバー折りたたみ

- サイドバーに ◀/▶ トグルボタンを追加
- 折りたたみ時は幅48pxのスリムな帯に縮小
- 折りたたみ中も総タスク・完了・期限超過の3件をミニ表示
- 状態はブラウザリロード後も保持

---

## v1.2.0 — ガントチャート強化・タスクNo・CSVインポート

- **タスクNo表示**：リストビューに専用「No」列を追加（`1`, `1.1`, `2.3.1` 形式）
- **ガントチャートにタスクNo表示**：ラベル列左にIDを小さく表示
- **ガントチャート依存関係点線矢印**：前タスクのバー右端→後タスクのバー左端へSVGでL字エルボーパス描画。`stroke-dasharray` の点線＋矢印マーカー付き
- **CSVインポート機能**：エクスポートと同一フォーマットでインポート可能。列名で照合するため列順不同でもOK。IDから親子関係を自動推定

---

## v1.3.0 — ガントチャートレイアウト修正・今日ライン

- ガントチャートのNo・タスク名列とカレンダーが食い込む問題を修正
- タスク名列（固定幅300px）とカレンダーエリアを完全分離した2カラム構造に変更
- **今日ライン**：カレンダーエリアの高さ全体にまたがる青い縦線を追加
- 「TODAY」バッジを今日ラインの上部に表示

---

## v1.4.0 — 階層操作UX改善

- 行左端のボタンを常時表示（ホバー依存をやめる）
  - `＋子`（青）：子タスク追加
  - `＋同列`（グレー）：同階層追加
  - `✕`：削除
- **タスク追加モーダルの文脈バナー**：青いバナーで親タスクのパンくずパス表示
- **右クリックコンテキストメニュー**：同階層追加 / 子タスク追加 / 詳細編集 / 削除
- ツールバーにヒントテキスト常時表示

---

## v1.5.0 — IDシステム刷新・フォルダ/タスク区別

- **内部IDをUUID方式に変更**：`t1`, `t2`… の連番（表示番号と分離）
- 表示番号（`1`, `1.1`…）は位置から動的に計算（階層変更時に自動振り直し）
- **フォルダ / タスクの区別を明示**：`type: 'folder' | 'task'` フィールドを追加
- 追加モーダルに「種別」セレクト（📁 フォルダ / 📄 タスク）
- フォルダには `FOLDER` バッジ（青）、タスクには `TASK` バッジ（グレー）
- フォルダ行に薄青背景・ガントバーは細め・透明度高め
- 詳細パネルでも種別変更可能

---

## v1.6.0 — ガントチャートD&D・依存関係D&D

- **ガントバーのドラッグ移動**：バー中央をドラッグで開始日・終了日を同時にシフト
- **ガントバーのリサイズ**：左端で開始日、右端で終了日を変更
- ドラッグ中はツールチップに `YYYY-MM-DD → YYYY-MM-DD (N日間)` をリアルタイム表示
- 依存矢印もドラッグ中にライブ更新
- **依存関係のD&D設定**：ガントチャートのラベル行・リストビューの行をドラッグ→別行にドロップで依存付け
- 既存の依存に再ドロップで削除確認ダイアログ
- ドロップ先行に青い点線ハイライト＋プレビュー矢印

---

## v1.7.0 — 複数プロジェクト対応

- プロジェクトの新規作成・切替・削除機能
- ヘッダーにプロジェクトセレクタ（カラードット＋プロジェクト名）
- サイドバーにプロジェクト管理パネル（完了数/総数 カウント付き）
- 新規プロジェクトモーダル：名前・説明・カラー（10色）・テンプレート選択
- テンプレート：空白 または 基本WBS（要件定義〜リリースの4フェーズ）
- **localStorageによる全データ永続化**：ブラウザを閉じても状態が復元

---

## v1.8.0 — ユーザー管理・ログイン機能

- `users[]` データモデルを追加（id・名前・役割・アバターカラー・メモ）
- **ログイン画面**：初回起動時にユーザーカードをクリックするだけでログイン（パスワードなし）
- ヘッダー右端にアバター＋名前ボタン（クリックでユーザー切替パネル）
- ユーザー管理モーダル：追加・編集（インライン）・削除
- タスク追加時に現在のログインユーザーを担当者に自動セット
- デフォルト4ユーザー（田中 太郎・佐藤 花子・鈴木 一郎・山田 美咲）

---

## v1.9.0 — 行の並び替えD&D

- ツールバーに「⠿ 並替モード」トグルボタンを追加
- 並替モードON時：行左端に ⠿ ハンドルが出現
- ドロップ位置による挿入制御：
  - 上25%：同列の上に挿入
  - 下25%：同列の下に挿入
  - 中央50%：子として挿入
- フォルダごと移動時は子タスクも丸ごと移動
- 自分の子孫へのドロップは無視（循環防止）
- 並替後は表示番号を自動振り直し

---

## v1.10.0 — ガントチャート折りたたみ

- ガントチャートのタスク名列で▼/▶ボタンによるフォルダ単位の折りたたみ・展開
- ヘッダー左に全展開（▼）・全折りたたみ（▶）ボタンを追加
- 折りたたみ状態はリストビューと共有
- 横スクロール位置をトグル操作で保持

---

## v1.11.0 — 依存関係の重複警告

- 前工程の終了日 ≥ 後工程の開始日 のとき「期間重複」と判定
- 重複時：赤い点線矢印（`#DC2626`）＋赤い矢印マーカー
- 通常時：グレー点線矢印
- バードラッグ中もリアルタイムで色が切り替わる
- サイドバー凡例に関係線の説明をSVGミニグラフで追加

---

## v1.12.0 — フォルダ日程矛盾の検知

- 全子孫タスクを対象として日程矛盾を検知（3条件）
  - 子孫の開始日 ＜ フォルダの開始日
  - 子孫の終了日 ＞ フォルダの終了日
  - 子孫の開始日 ＞ フォルダの終了日
- **リスト**：フォルダ行に `⚠ N件` バッジ＋薄赤背景、タスク行に `⚠` バッジ＋日付セル赤背景
- **ガント**：ラベル列に `⚠` アイコン、バーに赤いアウトライン枠線
- **サイドバー**：矛盾件数カード（矛盾がある場合のみ表示）

---

## v1.13.0 — CSVストレージモード（OneDriveフォルダ指定）

- File System Access API を使用したローカルフォルダへの自動保存
- ストレージモードを `localStorage` / `csvFile` で切り替え可能
- **参照モード / 編集モード**のチェックイン・チェックアウト方式
  - 起動時は常に参照モード（読み取り専用）
  - 「✏️ 編集開始」でチェックイン（ロックファイル書き込み）
  - 「✅ 編集完了」でチェックアウト（ロックファイルクリア）
- 30秒ポーリングで他ユーザーの編集状態・データ更新を確認
- 参照モード中はデータ更新を自動リロード
- ストレージインジケーターをヘッダーに常時表示
- Chrome / Edge のみ対応

---

## v1.14.0 — 内部ID付きCSV入出力

- エクスポートに `ID`・`依存関係（ID）`・`依存関係（No）`・`親ID` 列を追加
- インポート時に内部ID列を検出した場合は**アップサート**（ID一致→更新、新規ID→追加）または上書きを選択可能
- 依存関係の解決優先順位：内部ID列 → No列 → 旧形式列
- 旧フォーマットCSVとの後方互換性を維持
- インポート時に日付を `YYYY-MM-DD` に自動正規化

---

## v1.15.0 — ガントチャートスケール切替

- 日 / 週 / 月 / 四半期 / 年の5スケールに対応
- 列幅：日28px × 1 / 週×2 / 月×4 / 四半期×8 / 年×12
- スケール切替時に Today が画面横中央に自動スクロール
- 上位・下位の2段ヘッダー（例：月 + 日、年 + 月）

---

## v1.16.0 — フォルダ進捗自動計算・イナズマ線

- **フォルダ進捗自動計算**：全子孫タスクの期間加重平均で自動計算
- **イナズマ線**：「⚡ イナズマ線」ボタンでON/OFF切替
  - 対象：期間進行中または期限超過・未完了タスク（フォルダ・完了済み・将来タスクは除外）
  - 始点・終点に ◆ 菱形アンカー（Today ライン上）
  - 実績点が Today ラインより右→青、左→赤
  - 期間超過・未完了タスクは ◆ 菱形マーカーで強調

---

## v1.17.0 — ガントチャートスクロール固定

- ガントチャートを単一スクロールコンテナ構造に変更
- タスク名列を `position:sticky; left:0` で横スクロール時に左固定
- ヘッダー行を `position:sticky; top:0` で縦スクロール時に上固定
- 交差点セルを `top:0; left:0` で両方向固定
- ガントチャート開時に Today が画面中央に自動スクロール

---

## v1.18.0 — 担当者UI改善・OneDriveフォルダ記憶

- **担当者フィールド改善**：登録ユーザーのセレクトボックス（役割付き）＋直接入力テキストの2段構成
- セレクトとテキスト入力が相互に同期
- **OneDriveフォルダの記憶**：IndexedDB に `FileSystemDirectoryHandle` を保存
- 2回目以降は「⚡ ワンクリックで再接続」ボタンで許可クリックのみで接続完了
- フォルダ名ヒントを localStorage にも保存してUI表示に活用

---

## v1.19.0 — 操作マニュアル（ヘルプモーダル）

- ヘッダーに「❓ ヘルプ」ボタンを追加
- タブ付きヘルプモーダル（5タブ）：
  - 📋 事前準備：SharePoint→OneDriveショートカット追加手順（5ステップ）
  - 🚀 基本操作：タスク追加・並替・依存関係・CSV操作
  - 📊 ガントチャート：スケール・バー操作・矢印・イナズマ線
  - ☁️ OneDrive共有：初回接続・再接続・編集モード説明
  - 💡 Tips：矛盾検知・進捗自動計算・キーボードショートカット・CSVフォーマット

---

## v1.20.0 — 子タスク追加時の日程継承

- 親タスクに開始日・終了日が設定されている場合、子タスク追加モーダルに自動継承
- 親タスクの日程が未設定の場合は空欄のまま
- モーダル内で親タスクを変更した場合も選択後の親の日程に更新

---

## v1.21.0 — アクションボタンを左端に移動・アイコン化

- タスク追加・削除ボタンを右端から**最左端（Noの更に左）**に移動
- 各ボタンをSVGアイコンに変更：
  - 同列追加：行に＋が付くアイコン（グレー）
  - 子追加：インデントされた行のアイコン（青）
  - 削除：ゴミ箱アイコン（ホバーで赤く変化）
- 並替モードON時はアクションボタンを非表示、ハンドルのみ表示

---

## v1.22.0 — クリーンアップ・ドキュメント整備（GitHub公開準備）

- ArangoDB トライアル機能を完全削除（約270行のコード削除）
- ストレージ選択肢を「ローカルストレージ」「OneDrive（フォルダ指定）」の2択に整理
- 重複していた `saveToLS` 関数を1つに統合
- `README.md` 作成（セットアップ手順・OneDrive共有手順・CSVフォーマット説明）
- `DESIGN.md` 作成（機能一覧・機能概要・データモデル・技術構成）
- `CHANGELOG.md` 作成（本ファイル）

---

## v1.23.0 — ストレージ・排他制御の安定化

- **起動時ウィザード**：ストレージ選択（ローカル / OneDrive）とユーザー選択を1画面に統合（Step1→Step2のステップ式）
- **参照モードの完全ガード**：CSVモード起動時は `storageMode` の確定タイミングを修正し、フォルダ未接続でも参照モードを正しく維持
- **詳細パネルの参照モードロック**：入力フィールド全種（input / select / textarea / range）を `pointer-events:none` でロック。パネル上部に「👁 参照モード」バナーを表示
- **プロジェクト追加・削除の参照モードガード**：参照モード時はプロジェクト操作をブロックしトースト通知を表示
- **排他制御バグ修正**：`saveToCsvDir` が独自フォーマットでロックファイルを上書きしていた問題を修正。`writeLockFile` に統一し `locked` フィールドを必ず含める設計に変更
- **チェックアウト後のロック残存バグ修正**：`checkLockFile` に `!lock.userId` ガードを追加。`locked:false` 時の `clearedAt` タイムスタンプを追記
- **OneDriveフォルダハンドル永続化**：IndexedDB に `FileSystemDirectoryHandle` を保存しワンクリック再接続を実現
- **localStorageへの誤書き込み防止**：参照モード中は localStorage へも書き込まないよう `saveToLS` を修正

---

## v1.24.0 — UI・UX改善

- **ヘッダー整理**：ロゴ右のプロジェクトセレクタ・追加ボタンを削除（サイドバーに統合）
- **ストレージインジケーター改善**：接続中はフォルダ名表示のみ（クリック不可）、未接続時のみ `⚠ 未接続`（赤・クリックで設定を開く）
- **ユーザーボタン**：名前テキストを削除してアイコン（丸アバター）のみ表示
- **タブボタン固定幅化**：「リスト」「ガントチャート」ボタンを `width:130px` で固定し、画面幅による縦崩れを防止
- **ガントチャートツールバー**：スケール切替（日/週/月/四半期/年）とイナズマ線ボタンをヘッダーからガントビュー内の専用サブバーに移動
- **エクスポート・インポートボタン**：テキストラベルを削除しSVGアイコンのみに変更（エクスポート=上向き矢印、インポート=下向き矢印）
- **詳細パネルのコンパクト化**：幅320pxに縮小・2カラムグリッドレイアウト導入（種別/ステータス横並び、開始日/終了日横並び）・textareaを小さく・セクション区切り廃止
- **担当者フィールド統合**：セレクトボックス＋テキスト入力の2ボックス構成を `<input list>` + `<datalist>` の1ボックスに統合
- **リストヘッダー上部ズレ修正**：`.wbs-container` の `padding-top:0` に修正しスクロール時にヘッダーが正しく最上部に固定されるよう修正

---

## v1.25.0 — 機能追加

- **タスク非表示機能**：行アクションに👁アイコンを追加。クリックでタスクに `hidden:true` フラグを付与し、リスト・ガントチャート両方から非表示化。ツールバーに「🚫 非表示タスクを表示（N件）」チェックボックスを追加し、ON時に半透明で一覧表示・個別の再表示が可能
- **ステータスフィルタに「完了以外」追加**：未着手・進行中・ブロック中を一括表示するフィルタ選択肢を追加
- **ガントチャート選択時スクロール**：タスク名をクリックするとそのタスクの開始日が画面左1/4に来るよう自動スクロール
- **ガントチャートバー位置精度改善**：月・四半期・年スケールで日付の比率計算を正確化。`taskLeft` / `taskRight` を「日付の正確な左端ピクセル位置」に統一
- **四半期スケールを4月始まり年度に変更**：Q1=4〜6月, Q2=7〜9月, Q3=10〜12月, Q4=翌年1〜3月。ヘッダー上段を「YYYY年度」表示に変更
- **起動ウィザードのユーザーアイコン統一**：起動画面のユーザー選択カードをメイン画面と同じ丸アバターデザインに統一

---

## v1.26.0 — 起動ウィザード統合

- **起動ウィザードをStep1に統合**：OneDriveの⚡ワンクリック再接続ボタンをStep1（ストレージ選択画面）に配置し、Step2（ユーザー選択）への画面遷移を最小化
- **前回ユーザーの自動ログイン**：前回ログインしたユーザーが有効な場合、モード選択後にユーザー選択画面をスキップして自動ログイン
- **起動フローの整理**：初回・前回ユーザーなし・フォルダ未接続の場合のみStep2（ユーザー選択）を表示
- **フォルダ選択キャンセル時のフォールバック**：OneDriveフォルダ選択をキャンセルした場合、ローカルストレージモードにフォールバック

---

## v1.27.0 — 参照モード・排他制御の強化

- **参照モードのガード範囲拡大**：`updateEditModeUI` の判定を `storageMode==='csvFile'` のみに変更（`csvDirHandle` の有無に依存しない設計に修正）。フォルダ未接続の起動時でも確実に参照モードが適用される
- **プロジェクト追加・削除の参照モードガード**：参照モード時はプロジェクト操作をブロックしトースト通知を表示。ヘッダー・サイドバー両方のボタンをグレーアウト
- **`saveToLS` の参照モードブロック強化**：CSVモードの参照モード中はlocalStorageへの書き込みを完全にブロック（タイムスタンプ更新による誤判定を防止）
- **排他制御バグ修正**：`saveToCsvDir` が独自フォーマットでロックファイルを上書きしていた問題を修正。`writeLockFile` に統一し `locked` フィールドを必ず含める設計に変更

---

## v1.28.0 — UI細部の改善

- **サイドバー折りたたみボタンの重なり修正**：`position:absolute` を廃止し、「新規」ボタンの右隣にインライン配置することで重なりを解消
- **ガントチャートスクロールヘッダー固定**：単一スクロールコンテナ構造で左カラム・上ヘッダーのstickyを実現。縦スクロール時もタスク名列とカレンダーヘッダーが固定表示
- **イナズマ線のスケール追従修正**：`buildLightningLineHTML` 内の座標計算を `ganttScaleDateToX` に統一。週・月・四半期・年スケール切替時にイナズマ線が正しく追従するよう修正
- **リストヘッダーの上部ズレ修正**：`.wbs-container` の `padding-top:0` 設定でスクロール時のヘッダー位置ズレを修正
- **ガントチャートタスク名選択でスクロール**：タスク名クリック時に開始日が画面左1/4に来るよう自動スクロール

---

## v1.29.0 — ドキュメント・コード整理

- **ヘルプモーダルを全面更新**：起動ウィザード・タスク非表示・ステータスフィルタ・四半期4月始まり・イナズマ線スケール追従・参照モードガード範囲など最新機能を全5タブに反映
- **旧ログイン画面関連コードを削除**：`quickReconnect`（旧）・`reconnectCsvAndLogin`・`updateLoginCsvBanner`・`renderLoginScreen` の4関数を削除（起動ウィザードへの統合により不要）
- **コードクリーンアップ**：重複関数なし・未定義関数参照なし・ArangoDB残骸なしを確認（4,461行）
- **設計書・変更履歴・READMEを v1.29.0 に更新**

---

## v1.30.0 — 機能強化（ガントチャート・品質改善）

- **タスクバー色分けルール**：ガントチャートサブバーに「色分け：」セレクタを追加。ステータス／担当者／遅延状況／なしの4ルールでバー色を自動設定
- **ガントチャート印刷/PDF出力**：サブバーに「🖨 印刷/PDF」ボタンを追加。A3横向きで印刷プレビューを表示
- **クリティカルパス表示**：CPM法（前向きES/EF・後向きLS/LF・フロート=0判定）でクリティカルパスを自動計算。ガントバーに赤アウトライン・ラベル列に赤左ボーダー
- **ガントチャートフィルタ連動**：リストビューのタスク名検索・ステータス・担当者フィルタがガントチャートの行にも適用
- **ガントツールバーボタン順序変更**：①表示レンジ ②イナズマ線 ③クリティカルパス ④色分け ⑤印刷の順に整理
- **担当者アバターをガントラベル列に表示**：タスク名の右に担当者の丸アバターアイコンを表示
- **マイルストーン機能**：type='milestone' を追加。ガントチャートで◆ひし形マーカー（紫）、リストで🔷アイコン＋MILESTONEバッジ（紫）
- **タスク追加時の挿入位置修正**：`tasks.push` による末尾追加を廃止。`getSubtreeLastIndex` で正しい挿入位置を計算し、クリックした行のサブツリー直後に挿入
- **IDカウンタ衝突バグ修正**：サンプルデータのID（t100〜t116）と新規追加IDが衝突する問題を修正。`_uid` 初期値を200に変更、`resyncUid()` で既存データ読み込み後に安全な値に自動補正

---

## v1.31.0 — セキュリティ・整合性強化

- **XSS対策強化**：`escHtml()` に `"` と `'` のエスケープを追加。タスク名・担当者名・プロジェクト名・ユーザー名など全innerHTML出力箇所に適用。ロックバナーの `lockedBy` もエスケープ対象に追加
- **データバリデーション**：`validateWbsData()` 関数を新設。`loadFromLS`・`loadFromCsvDir` の両方でJSONスキーマ検証を実施。不正データによるクラッシュを防止
- **参照モードガード節の追加**：`isReadOnly()` / `guardReadOnly()` ヘルパーを新設。`deleteTask`・`onDepLinkDrop`・`onListDepDrop`・`submitAddTask`・`applySort`・`dpUpdate`・`dpUpdateDeps`・`importCSV`・ガントバードラッグの各関数冒頭にガード節を追加
- **保存漏れバグ修正**：`applySort`・`onDepLinkDrop`・`onListDepDrop`・ガントバードラッグ完了時に `saveToLS()` が未呼び出しだった問題を修正
- **UndoロジックのタイミングBug修正**：`dpUpdate` のUndoスナップショットを変更前に取得するよう修正。同一フィールド連続編集は1件にまとめる設計に変更
- **SVG行アクションのHTML破壊バグ修正**：非表示トグルボタンのSVGインライン条件分岐内の `"` がHTMLパーサーを混乱させていた問題を修正。SVGをテンプレートリテラル外に分離
- **同列追加ボタンの parentId 修正**：`parentId||''` が空文字になる問題を `parentId?'id':'null'` の明示分岐に修正

---

## v1.32.0 — ダッシュボード機能大幅強化

- **ダッシュボードタブ追加**：ヘッダーに「🏠 ダッシュボード」タブを追加
- **🏥 スケジュール健全性スコア**：0〜100点のゲージで進捗遅延・期限超過・ブロックを減点評価。🟢健全 / 🟡注意 / 🔴危険の3段階判定
- **⚠ 遅延リスクタスク一覧**：計画進捗率と実績進捗率の差が10%以上のタスクを抽出して一覧表示
- **📅 今週の作業予定**：今日から7日以内に開始・終了・進行中のタスクを終了日順で一覧表示
- **📉 バーンダウンチャート**：SVG `viewBox` によるレスポンシブ描画。理想ライン・実績ライン・今日線を表示。カード幅に追従してグラフが拡大
- **🔴 クリティカルパス一覧**：ダッシュボード独立のCPM計算。ガント表示ON/OFFに依存せずCPタスクを一覧表示
- **📊 フェーズ別進捗タイムライン**：最上位フォルダをフェーズとして横棒タイムラインで表示。今日線・実績進捗バー・期限超過マーク付き
- **⚖ チームワークロード分析**：担当者ごとの進行中タスク数・今週締切数・期限超過数を表示。高負荷/適正/余裕を自動判定
- **🕐 直近の更新履歴**：Undo履歴ログを活用して最新15件の変更を操作者アバター付きで表示
- **カード表示設定**：表示/非表示・幅（標準1列/2列）・並べ替え（ドラッグ）をモーダルで設定。設定はlocalStorageに保存しリロード後も維持

---

## v1.33.0 — 変更履歴・Undo機能

- **変更履歴モーダル**：ヘッダーの🕐ボタンで最大200件の操作ログを日時・内容・操作者付きで表示
- **Undo機能**：Ctrl+Z で最大30件Undo。ヘッダーの↩ボタンでも実行可。入力欄フォーカス中はブラウザ標準Undoを優先
- **`pushUndo` 対象操作**：タスク追加・削除・フィールド編集・依存関係変更・並べ替え・日程ドラッグ

---

## v1.34.0 — 改善要望管理タブ

- **📝 改善要望タブ**：管理番号（自動連番）・ステータス（未対応/対応中/対応済/却下）・改善要望・要求者・記載日・対応結果・対応者・対応日時を管理
- **OneDrive保存対応**：`wbs_feedback.json` として `wbs_data.json` と同フォルダに別ファイルで保存
- **ステータスフィルタ**：未対応/対応中/対応済/却下で絞り込み可能
- **参照モード中は操作ブロック**：追加・編集・削除を参照モード中はガード

---

## v1.35.0 — サイドバー大改修・ヘッダー整理

- **サイドバー全面再設計**：プロジェクト選択セレクト・表示切替ボタン・フィルタ・今日のタスク・遅延タスクの5セクション構成に変更
- **表示切替をサイドバーへ移動**：リスト／ガント／ダッシュボード／改善要望のタブボタンをサイドバーの「🖥 表示」セクションに移動
- **フィルタをサイドバーへ統合**：テキスト検索・担当者・自分のタスク・未完了のみ・遅延のみ・マイルストーンのみ・非表示の7フィルタをサイドバーに集約。リスト・ガント両方に連動
- **今日のタスク・遅延タスク**：`updateStats()` でリアルタイム計算。自分のタスクを優先表示。各行クリックでリストビューに遷移
- **プロジェクト削除ボタン**：セレクト横の🗑ボタンを追加。1件のみの場合はグレーアウト
- **サイドバー折りたたみ時の表示切替**：折りたたみ中も📋📊🏠📝アイコンボタンで表示切替可能
- **ヘッダーのシンプル化**：タスク追加・Undo・履歴をヘッダー左側に配置。エクスポート・インポート・ヘルプ・ユーザーをヘッダー右側に配置
- **プロジェクト切替時のフィルタ自動リセット**：切替時に全フィルタ・選択状態・折りたたみ・詳細パネルをリセット
- **ツールバー廃止**：ヘッダー直下のツールバー（検索・フィルタ・ドロップダウン）を廃止してサイドバーに統合。タスク表示領域を拡大

---

## v1.36.0 — セキュリティ・整合性・バグ修正

- **CSS重複定義の解消**：旧サイドバーCSS（`.sidebar`・`.sidebar-content`・`.sidebar-collapsed-icons`）が新CSSと二重定義されていた問題を修正
- **デッドコードの削除**：`sidebar-proj-list`・`assignee-legend`・`header-user-name`・`sb-conflict-row` への参照コードを削除
- **`toggleSidebar` の修正**：固定ID `sidebar-toggle` への参照を `querySelectorAll('.sidebar-toggle')` に変更（新デザインで複数ボタンが存在するため）
- **renderAll のダッシュボード再描画漏れ修正**：`else renderGantt()` だったため、ダッシュボードタブ表示中にタスク変更してもダッシュボードが更新されなかった問題を修正
- **importCSV の resyncUid 追加**：インポート後に `resyncUid()` と `saveToLS()` を明示的に呼び出してIDカウンタの衝突を防止
- **D&Dバリデーション強化**：`applySort` に自分自身・子孫への移動を禁止するガードを3層追加。不正操作時はトースト通知を表示

---

## v1.37.0 — ヘッダーレイアウト最終化・編集モードバッジ

- **ヘッダー並び順の確定**：左から「ロゴ／参照モードバナー／編集中バッジ／編集開始・完了ボタン／ストレージ表示／区切り／＋タスク追加／↩Undo／🕐履歴」→スペーサー→「↑エクスポート／↓インポート／❓ヘルプ／区切り／👤ユーザー」
- **✏️ 編集中バッジ追加**：OneDriveモードで編集開始後、緑の「✏️ 編集中」バッジをヘッダーに表示。参照モード（黄）・他ユーザー編集中（赤）と合わせて3状態を視覚化
- **折りたたみ時の表示切替ボタン追加**：サイドバー折りたたみ時に📋📊🏠📝の4ボタンを縦並び表示。アクティブタブを青ハイライト
- **ガントチャートレンジ幅チューニング**：日=40px（約30日）・週=90px（約3ヶ月）・月=100px（約1年）・四半期=200px（約1.5年）・年=400px（約3年）に変更

---

## v1.38.0 — 起動ウィザード UX改善

- **起動ウィザードの2ボタン問題を解消**：OneDriveモードで前回フォルダがある場合に「前回と同じ設定で続行」と「⚡ ワンクリックで再接続」の2ボタンが同時表示されて混乱する問題を修正
- **1ボタンに統合**：状況に応じて表示内容が自動的に変化する続行ボタン1つに統合
  - OneDrive＋フォルダ記憶あり → 「⚡ OneDriveに接続して続行」（緑）
  - OneDrive＋フォルダ未記憶 → 「☁️ OneDriveモードで続行」
  - ローカル → 「💾 ローカルストレージで続行」
- **バグ修正**：`startupQuickReconnect()` が削除済みの `startup-quick-section` 要素を参照してエラーが発生し先に進めなかった問題を修正

---

## v1.39.0 — コード品質向上・バグ修正

- **不要コード削除**：`updateStats` の `assignee-legend` 参照・`updateUserUI` の `header-user-name` 参照を削除
- **renderAll のタブ分岐修正**：ダッシュボード・フィードバックタブの正確な分岐を実装
- **プロジェクト切替フィルタリセット**：`switchProject` にフィルタ全リセット処理を追加（テキスト検索・ステータス・担当者・チェックボックス4つ・非表示チェック）
- **import後のID整合性**：`importCSV` 完了後に `resyncUid()` を呼び、インポートデータのIDとの衝突を防止。`saveToLS()` を明示的に追加
- **ヘルプコンテンツ最新化**：起動ウィザード（1ボタン統合）・ヘッダーレイアウト・フィルタのプロジェクト切替リセット・サイドバー折りたたみ切替を全5タブに反映

---

## v1.40.0 — バックアップ機能・バージョン表示

- **OneDriveバックアップ機能**：「✏️ 編集開始」を押すたびに `Backup/タイムスタンプ/` フォルダにデータを自動保存
  - 保存先：OneDriveフォルダ内 `Backup/` サブフォルダ（自動作成）
  - 保存ファイル：`wbs_data.json`（タスクデータ）＋ `wbs_feedback.json`（改善要望）
  - 最大10世代を保持。超過分は古い順に自動削除
  - バックアップ失敗時も編集モード移行はブロックしない。失敗時はトースト警告を表示
- **バージョン表示追加**：ヘッダーのロゴ右横に `v1.40.0` バッジを常時表示
- **ヘルプモーダルにフッター追加**：全タブ共通フッターにバージョン番号と「閉じる」ボタンを配置
- **ヘルプコンテンツ最新化**：OneDrive共有タブに「📦 自動バックアップ」セクションを追加。Tipsタブに「バックアップからの復元」手順を追加

---

## v1.41.0 — ガントチャート・CSV・UI改善

### ガントチャート
- **ラベル列幅リサイズ**：ラベル列右端をドラッグして160px〜600pxの範囲で自由に調整可能。幅はセッション中保持
- **リサイズハンドル**：透明な8px幅のドラッグハンドルをラベルヘッダー右端に追加。カーソルが `col-resize` に変化

### CSV インポート・エクスポート
- **種別の英語表記対応**：エクスポートを `task`/`folder`/`milestone` の英語表記に変更（従来は「タスク」「フォルダ」の日本語表記）
- **マイルストーンのCSV対応**：エクスポート時にマイルストーンが「タスク」になっていたバグを修正。インポート時も `milestone`/`マイルストーン` を正しく認識
- **ステータスの英語表記対応**：エクスポートを `todo`/`doing`/`done`/`block` の英語表記に変更
- **大文字小文字・日本語を吸収**：インポート時に `Todo`/`TODO`/`未着手`/`Folder`/`MILESTONE` 等を全て正規化して認識

### 詳細パネルの格納UI
- **格納帯ボタン**：パネル右端の `✕` ボタンを廃止し、パネル左端に「`›`」形状の格納帯を追加
- ホバーでアクセントカラーに変化。パネル外にはみ出して常時表示
- `overflow:hidden` → `overflow:visible` に変更してパネル外への帯表示を実現

### プロジェクト編集
- **編集ボタン追加**：サイドバーのプロジェクトセレクト横に `✏️` 編集ボタンを追加（🗑削除ボタンの左）
- **編集モーダル**：プロジェクト名・説明・カラーを変更できるモーダルを新設。現在の値が自動的にセットされた状態で開く
- 参照モード中はグレーアウト・クリック不可

---

## v1.42.0 — WBS雛形CSV整備

- **大規模システム開発WBS雛形CSV更新**（`wbs_template_system_dev.csv`）
  - タスク間の依存関係を全204タスク中177件に追加（工程間・工程内の前後関係を反映）
  - 種別を英語表記（`task`/`folder`/`milestone`）に統一
  - ステータスを英語表記（`todo`）に統一
  - 受入テスト：第1・第2ラウンドの問題管理→修正→再テストサイクルの依存を整備
  - クリティカルパス：起案→構想→業務要求→システム要件→外部設計→詳細設計→実装→結合→システム→受入/運用→リリース→安定化フォローの一連の依存関係を設定
  - 並行工程（データ移行・社外提供準備・展開準備）の依存関係を整備

---

## v1.43.0 — ガントチャートヘッダー固定・コード品質修正

### ガントチャート
- **ヘッダー左端固定バグ修正**：横スクロール時にタスク名ヘッダー（交差点セル）が流れてしまう問題を修正。`position:sticky;left:0` と `position:relative` を同一要素に二重指定していたため `relative` が優先され左固定が無効化されていた。外側ラッパー（`sticky:left:0`）と内側ラッパー（`position:relative`）に分離して解決
- 縦スクロール：日付ヘッダーが上端に固定（変更なし）
- 横スクロール：タスク名列・タスク名ヘッダーが左端に固定（修正済み）

### コード品質
- **`resyncUid` のタイミング修正**：`switchProject` でプロジェクトを切り替えた際に `syncTasks()` 実行後に `resyncUid()` を呼ぶよう修正。切替先プロジェクト内の既存タスクIDと新規タスクIDの衝突リスクを排除
- `applyLoadedData`・CSVインポート後の `resyncUid()` 呼び出しは既に実装済みで変更なし

### ヘルプコンテンツ・バージョン表示
- **バージョンを v1.42.0 に更新**：ヘッダーバッジ・ヘルプフッター
- **ガントチャートタブ更新**：「ヘッダー・ラベル列の固定」セクションを表形式で整理（縦スクロール/横スクロール別に説明）
- 全5タブの内容が最新機能と一致していることを確認・整合


---

## v1.44.0 — ガントチャート改善・バグ修正・パフォーマンス改善

### ガントチャート
- **ドラッグ移動のスケール対応**：バー移動量をレンジ（日/週/月/四半期/年）に応じた正確なpx/日で計算するよう修正。`dragDxToDeltaDays()` 関数を新設し、`ganttScaleDateToX()` を使ったスケール歪み補正も実装
- **バー座標をスケール統一**：ドラッグ中・復元時のバー座標計算を `dateToOff()*DAY_W` から `ganttScaleDateToX()` ベースに統一
- **ドラッグ中スクロール位置保持**：バー移動確定後に上下（scrollTop）・左右（scrollLeft）スクロール位置を両方保存・復元。タスクの選択状態を維持したまま画面位置が追従
- **バーが消える問題を修正**：`moved=false`・`deltaDays=0` のケースでバーを元座標に確実に復元。ドラッグ中に一時移動したバーが放した後に消える問題を解消

### バグ修正
- **詳細パネルの日付入力フリーズを修正**：`dp-start`・`dp-end` の日付フィールドを `oninput` から `onchange` に変更。キーボード直接入力中の途中発火による無限ループを根本解決
- **dpUpdate のデバウンス強化**：OneDrive保存を300msデバウンスに変更し、localStorageへの即時保存と分離。日付ピッカー連続操作でのUIフリーズを解消
- **非表示タスクフィルターが消える問題を修正**：`updateHiddenBtn()` を `updateStats()` 内で呼ぶよう変更。`renderAll()` のたびに非表示件数と表示状態が正しく更新されるようになった

### パフォーマンス改善（大規模プロジェクト対応）
- **バックアップの非同期バックグラウンド化**：`checkin()` でバックアップを `await` せず並行実行。ロック取得・編集開始を先に完了させることで体感速度を大幅改善（300件以上のプロジェクトで効果あり）
- **JSON整形なし化**：`saveToCsvDir()`・`runBackup()` の `JSON.stringify(data, null, 2)` を `JSON.stringify(data)` に変更。300タスクで約200KB → 約80KBに削減（約60%）。書き込み・読み込み速度を改善

